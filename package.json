{
  "name": "jetlag-la",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "python scripts/build_map.py",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:refresh": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
